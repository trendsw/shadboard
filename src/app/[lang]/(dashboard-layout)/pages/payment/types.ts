export interface CardType {
  id: string;
  cardType?: string;
  cardNumber?: string;
  cardName: string;
  expiry?: string;
  cvc?: string;
  accountNumber?: string;
  routingNumber?: string;
  last4?: string;
}
